name: Build compiler and runtime

on:
  workflow_call:
    inputs:
      push:
        description: Push the built docker images to the registry
        required: false
        default: 'false'
        type: string

    outputs:
      compiler_wheel_artifact_id:
        description: ID of the artifact with the compiler wheel to download
        value: ${{ jobs.build.outputs.compiler_wheel_artifact_id }}

      runtime_wheel_artifact_id:
        description: ID of the artifact with the runtime wheel to download
        value: ${{ jobs.build.outputs.runtime_wheel_artifact_id }}

      user_manual_artifact_id:
        description: ID of the artifact with the built user manual to download
        value: ${{ jobs.build_docs.outputs.user_manual_artifact_id }}
      
      website_artifact_id:
        description: ID of the artifact with the built website to download
        value: ${{ jobs.build_docs.outputs.website_artifact_id }}
      release_artifact_id:
        description: ID of the artifact with the built release
        value: ${{ jobs.assemble_release.outputs.release_artifact_id }}

    secrets:
      HF_TOKEN:
        required: true

permissions:
  contents: read
  id-token: write  
  packages: write
  attestations: write

jobs:

  docker_image_builder:
    name: Create Builder docker
    uses: ./.github/workflows/build-docker-image-builder.yml
    with:
      push: ${{ inputs.push }}

  build_mpact:
    name: Build Mpact
    uses: ./.github/workflows/build-mpact.yml
   
  build_ko_dependencies:
    name: Build KO dependencies
    uses: ./.github/workflows/build-ko-dependencies.yml

  build_docs:    
    name: Build documentation
    uses: ./.github/workflows/build-docs.yml

  build:
    name: Build compiler and runtime
    needs: [docker_image_builder, build_mpact]

    runs-on:
      - codebuild-${{ vars.CODEBUILD_PROJECT_CODEBASE }}-${{ github.run_id }}-${{ github.run_attempt }}
        image:custom-linux-${{ needs.docker_image_builder.outputs.image_name }}

    env:
      SCCACHE_BUCKET: ${{ vars.SCCACHE_BUCKET }}
      SCCACHE_REGION: ${{ vars.AWS_REGION }}
      SCCACHE_S3_KEY_PREFIX: clang-build

    timeout-minutes: 50 # ensure build jobs fail before CodeBuild kills the runners

    outputs:
      compiler_wheel_artifact_id: ${{ steps.upload_compiler_wheel.outputs.artifact-id }}
      runtime_wheel_artifact_id: ${{ steps.upload_runtime_wheel.outputs.artifact-id }}
      turbine_wheel_artifact_id: ${{ steps.upload_turbine_wheel.outputs.artifact-id }}

    steps:

      - name: Make cached checkout visible to workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # actions/checkout and uses keyword can only access files within the current workflow workspace
          ln -s /workspace/code .

          # ensure the github token is used by the checkout action for all operations (the action cannot handle this
          # because it assumes the checkout is not a symlink in the way it sets up authentication)
          AUTH=$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')
          git config --global "http.https://github.com/.extraheader" "AUTHORIZATION: basic $AUTH"

      - name: Update checkout
        uses: actions/checkout@v6
        with:
          path: code
          lfs: true

      - name: Update required submodules
        working-directory: /workspace/code
        run: |
          bash scripts/checkout_submodules.sh

      - name: Configure AWS credentials
        uses: ./code/.github/actions/configure-aws-credentials
        id: configure-aws-credentials
        with:
          aws_region: ${{ vars.AWS_REGION }}
          protected_role_arn: ${{ vars.MAIN_BRANCH_ROLE_ARN }}
          non_protected_role_arn: ${{ vars.PULL_REQUEST_ROLE_ARN }}

      - name: Download mpact simulation library from cache
        uses: actions/cache/restore@v5
        with:
          path: ${{ needs.build_mpact.outputs.path }}
          key: ${{ needs.build_mpact.outputs.cache_key }}
          fail-on-cache-miss: true

      - name: Uncompress mpact simulation library
        run: |
          mkdir -p /workspace/code/third_party/coralnpu-mpact/x86_64-Linux
          tar -xzf ${{ needs.build_mpact.outputs.path }} --strip-components=1 -C /workspace/code/third_party/coralnpu-mpact/x86_64-Linux

          find /workspace/code/third_party/coralnpu-mpact/x86_64-Linux

      - name: Configure build for host
        working-directory: /workspace/code
        run: |
          ./scripts/configure_build.sh /workspace/build

      - name: Build for host
        working-directory: /workspace/build
        env:
          SCCACHE_SERVER_UDS: ${{github.workspace}}/sccache.sock
        run: |
          sccache --start-server
          cmake --build . --target torq
          sccache --show-stats
      - name: Set tag variable
        id: vars
        working-directory: /workspace/code
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            REF_NAME=${{ github.ref_name }}
            echo "wheel_version=${REF_NAME#v}" >> $GITHUB_OUTPUT
            echo "tag_name=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          else
            TIMESTAMP="$(date -u '+%Y%m%dT%H%M%SZ')"
            COMMIT="$(git rev-parse --short HEAD 2>/dev/null || echo 'unknown')"
            echo "wheel_version=0.dev0+${TIMESTAMP}.g${COMMIT}" >> $GITHUB_OUTPUT
            echo "tag_name=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Build runtime wheels
        working-directory: /workspace/code
        run: |
          # Needed for torq.build_tools discovery.
          source /workspace/venv/bin/activate

          export TORQ_WHEEL_VERSION="${{ steps.vars.outputs.wheel_version }}"

          ./scripts/build_runtime_wheel.sh /workspace/build

      - name: Upload runtime wheels
        id: upload_runtime_wheel
        uses: actions/upload-artifact@v7
        with:
          name: runtime-wheel
          path: /workspace/code/dist/torq_runtime-*.whl
          retention-days: 30

      - name: Build compiler wheel
        working-directory: /workspace/code
        run: |
          # Needed for torq.build_tools discovery.
          source /workspace/venv/bin/activate

          export TORQ_WHEEL_VERSION="${{ steps.vars.outputs.wheel_version }}"

          ./scripts/build_compiler_wheel.sh /workspace/build

      - name: Upload compiler wheel
        id: upload_compiler_wheel
        uses: actions/upload-artifact@v7
        with:
          name: compiler-wheel
          path: /workspace/code/dist/torq_compiler-*.whl
          retention-days: 30

      - name: Build torq-turbine wheel
        working-directory: /workspace/code
        env:
          TORQ_WHEEL_VERSION: ${{ steps.vars.outputs.wheel_version }}
        run: |
          source /workspace/venv/bin/activate
          ./scripts/build_turbine_wheel.sh

      - name: Upload torq-turbine wheel
        id: upload_turbine_wheel
        uses: actions/upload-artifact@v7
        with:
          name: turbine-wheel
          path: /workspace/code/dist/torq_turbine-*.whl
          retention-days: 30

      - name: Package host build for release assembly
        working-directory: /workspace
        run: |
          cp /workspace/code/third_party/coralnpu-mpact/x86_64-Linux/libcoralnpu_simulator_mpact.so \
             /workspace/build/third_party/iree/lib/
          bash /workspace/code/scripts/package_host_release.sh /workspace/build /workspace/release-host
          tar -C /workspace/release-host -czf host-release.tar.gz .

      - name: Upload host build for release assembly
        uses: actions/upload-artifact@v7
        with:
          name: host-release
          path: /workspace/host-release.tar.gz
          retention-days: 1

      - name: Upload mpact simulation library as artifact
        uses: actions/upload-artifact@v7
        with:
          name: mpact-libs
          path: ${{ needs.build_mpact.outputs.path }}
          retention-days: ${{ inputs.push == 'true' && 30 || 1 }}

  build_docker:
    name: Build release docker image
    needs: assemble_release
    uses: ./.github/workflows/build-docker-image-release.yml
    with:
      release_artifact_id: ${{ needs.assemble_release.outputs.release_artifact_id }}
      push: ${{ inputs.push }}

  docker_image_builder_astra:
    name: Create Astra Builder docker
    uses: ./.github/workflows/build-docker-image-builder-astra.yml
    with:
      push: ${{ inputs.push }}

  build_astra:
    name: Build Astra machina
    needs: [docker_image_builder_astra, build_ko_dependencies]

    runs-on:
      - codebuild-${{ vars.CODEBUILD_PROJECT_CODEBASE }}-${{ github.run_id }}-${{ github.run_attempt }}
        image:custom-linux-${{ needs.docker_image_builder_astra.outputs.image_name }}

    steps:

      - name: Make cached checkout visible to workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ln -s /workspace/code .

          AUTH=$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')
          git config --global "http.https://github.com/.extraheader" "AUTHORIZATION: basic $AUTH"

      - name: Update checkout
        uses: actions/checkout@v6
        with:
          path: code
          lfs: true

      - name: Download ko dependencies from cache
        uses: actions/cache/restore@v5
        with:
          path: ${{ needs.build_ko_dependencies.outputs.path }}
          key: ${{ needs.build_ko_dependencies.outputs.cache_key }}
          fail-on-cache-miss: true

      - name: Configure build for Astra machina
        working-directory: /workspace/code
        run: |
          ./scripts/configure_soc_build.sh /workspace/build-astra-sl /workspace/build astra_machina poky

      - name: Build for Astra machina
        working-directory: /workspace/build-astra-sl
        run: |
          cmake --build . --target torq-run-module torq_kernel_module

      - name: Package Astra build artifacts
        working-directory: /workspace
        run: |
          bash /workspace/code/scripts/package_astra_release.sh /workspace/build-astra-sl /workspace/release-astra
          tar -C /workspace/release-astra -czf astra-release.tar.gz .

      - name: Upload Astra build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: astra-release
          path: /workspace/astra-release.tar.gz
          retention-days: 30

      - name: Upload KO dependencies as artifact
        uses: actions/upload-artifact@v7
        with:
          name: ko-dependencies
          path: ${{ needs.build_ko_dependencies.outputs.path }}
          retention-days: ${{ inputs.push == 'true' && 30 || 1 }}

  assemble_release:
    name: Assemble and upload release
    needs: [build, build_astra]
    runs-on: ubuntu-latest
    outputs:
      release_artifact_id: ${{ steps.upload_release.outputs.artifact-id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          path: code
          lfs: true

      - name: Download host release package
        uses: actions/download-artifact@v4
        with:
          name: host-release
          path: .

      - name: Download Astra release package
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: astra-release
          path: .

      - name: Merge release packages
        run: |
          mkdir -p release
          tar -xzf host-release.tar.gz  -C release
          if [ -f astra-release.tar.gz ]; then tar -xzf astra-release.tar.gz -C release; fi

      - name: Install minimal python deps for release assembly
        run: |
          python3 -m pip install --upgrade --target assemble_deps huggingface_hub

      - name: Gather release files
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          PYTHONPATH=assemble_deps ./code/scripts/assemble_release_docker.sh release

          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "${{ github.ref_name }}" > release/VERSION
          else
            echo "${{ github.sha }}" > release/VERSION
          fi

          tar -zcf release.tar.gz release

      - name: Upload release files
        id: upload_release
        uses: actions/upload-artifact@v7
        with:
          name: release
          path: release.tar.gz
          retention-days: 30
