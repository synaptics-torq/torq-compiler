# This file must contain only pinned versions of dependencies

# Python testing (matches ubuntu 24.04 packages)
pytest==7.4.4
pytest-xdist==3.4.0
pytest-json-report==1.5.0

# Connection to huggingface
huggingface_hub==1.3.2

# Used for apps
streamlit==1.54.0

# force tensorflow that is compatible with IREE version we use
# (iree has a >= dependency which is picking up 2.19.0 which is not compatible)
tensorflow==2.18.1

# for conversion from tflite to TOSA 1.0 compliant MLIR
tosa-converter-for-tflite==2026.2.0

# for TFLite layer extraction (tflite_layer_extractor.py)
flatbuffers>=24.3.25

# we depend on this specific version of keras at the moment
keras==3.12.0

# onnx
onnx==1.19.1
onnxruntime==1.25.0
onnxscript==0.6.1

# torch (used for bf16 reference execution in testing)
torch==2.11.0

# Used for image_to_tensor.py utility tools
opencv-python-headless==4.12.0.88

# for diff-tensors.py
matplotlib==3.10.5

# for gantt_plotly.py
plotly==6.3.0

# for annotate_profiling.py
xlsxwriter==3.2.5

# for documentation
# versions are based on latest https://github.com/syna-astra-dev/synaptics-sphinx-theme/blob/main/Dockerfile
# which uses a base image built with https://github.com/sphinx-doc/sphinx-docker-images/blob/7.4.7/base/Dockerfile

# base image packages
Sphinx==7.4.7
pillow==12.1.1

# head of synaptics theme
https://github.com/syna-astra-dev/synaptics-sphinx-theme/archive/refs/heads/main.zip

# this is to automatically rebuild upon edit the documentation
sphinx-autobuild==2025.8.25

# these are from third_party/iree/runtime/bindings/python/iree/runtime/build_requirements.txt
pip>=21.3
setuptools>=62.4.0
nanobind==2.0
numpy>=2.0.0b1
ml_dtypes>=0.4.0
requests>=2.28.0
wheel>=0.36.2
sympy==1.13.3
pybind11>=2.10.1
PyYAML>=5.4.1

# these are from third_party/iree/integrations/tensorflow/test/requirements.txt
tensorflow==2.18.1
keras>=2.7.0
# removed Pillow as we already list it above 

# these are from python/torq/requirements.txt
packaging>=24.2
